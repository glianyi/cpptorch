{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppdbg",
            "request": "launch",
            "name": "torch launch",
            "program": "${workspaceFolder}/build/cpptorch",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb"
        }
    ]
}